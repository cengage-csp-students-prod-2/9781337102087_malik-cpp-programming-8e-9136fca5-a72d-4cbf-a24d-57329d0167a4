#include <fstream>
#include <string>
#include <iomanip>

using namespace std;

int main()
{
    // create streams
    ifstream inFile;
    ofstream outFile;

    // open files
    inFile.open("inData.txt");
    outFile.open("outData.txt");

    //
}
